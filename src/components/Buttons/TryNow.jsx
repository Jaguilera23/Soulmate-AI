const TryNow = ({text}) => {
    return (
        <button type="button" className="px-8 py-4 rounded-full border-white font-Inter text-white border gap-2">{text}</button>
    )
}

export default TryNow